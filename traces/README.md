# Fortran Trace Assets

Raw server traces are development evidence and are not part of the normal JAX
runtime package. Public provenance ledgers remain under `docs/source_audits`;
large or restricted trace packages must be supplied separately.
